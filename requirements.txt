# Direct dependencies only. pip resolves compatible transitive versions
# (cryptography, paramiko, lxml, urllib3, ...) for whatever Python is in use,
# so this file works on both Python 3.8 and 3.12 lab runners.
ncclient>=0.6.13
pytest>=7.4
pytest-html>=4.1
xmltodict>=0.13
requests>=2.28
